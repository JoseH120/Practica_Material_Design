<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="personal.app.practica_material_design">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Practica_Material_Design">
        <activity android:name=".MostrarDatosActivity" android:exported="true" android:label="@string/strActivityDatos" />
        <activity android:name=".List_Employees_Activity" android:label="@string/strActivityLstEmployees"/>
        <activity android:name=".AddEmployeeActivity" android:label="@string/strActivityAddEmployee" />
        <activity android:name=".MainActivity" android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>